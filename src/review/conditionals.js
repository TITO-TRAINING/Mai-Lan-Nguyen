//if
let num = 3;
if (num > 0) {
  console.log(`${num} is a positive number`); //this part of code runs for truthy condition
}
//  3 is a positive number

// If Else
let num1 = 3;
if (num1 > 0) {
  console.log(`${num1} is a positive number`); // this part of code runs for truthy condition
} else {
  console.log(`${num1} is a negative number`); // this part of code runs for false condition
}
//  3 is a positive number

//Ternary
console.log(a === b ? 'true' : 'false');
